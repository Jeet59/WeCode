type User {
  username: String
  email: String
  password: String
}
type Query {
  hello: String
  user(username: String, password: String): User
  users: [User]
}
type Mutation {
  addUser(username: String, email: String, password: String): User
}
